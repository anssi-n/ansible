---
- name: Ensure github pat is provided
  ansible.builtin.fail:
    msg: Github PAT must be provided in boostrap_cluster_github_pat variable
  when: boostrap_cluster_github_pat is not defined

- name: Check if flux-system namespace exists
  ansible.builtin.command:
    cmd: kubectl get ns flux-system --ignore-not-found
  changed_when: false
  register: bootstrap_cluster_fluxcd_ns

- name: Set cluster Check if cluster if already boostrapped
  ansible.builtin.set_fact:
    bootstrap_cluster_boostrapped: "{{ (bootstrap_cluster_fluxcd_ns.stdout != '') | bool }}"

- name: Bootstrap cluster
  when: not bootstrap_cluster_boostrapped
  block:
    - name: Bootrap cluster
      ansible.builtin.command:
        cmd: >
          flux bootstrap github
          --owner="{{ bootstrap_cluster_github_owner }}"
          --repository="{{ bootstrap_cluster_github_repository }}"
          --private=false
          --personal=true
          --path="{{ bootstrap_cluster_github_repository_path }}"
      changed_when: false
      timeout: "{{ bootstrap_cluster_timeout }}"
      register: bootstrap_cluster_result
      environment:
        GITHUB_TOKEN: "{{ boostrap_cluster_github_pat }}"

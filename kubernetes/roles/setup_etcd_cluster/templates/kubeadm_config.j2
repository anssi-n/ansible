---
apiVersion: "kubeadm.k8s.io/v1beta4"
kind: InitConfiguration
nodeRegistration:
    name: "{{ etcd_names[idx] }}"
localAPIEndpoint:
    advertiseAddress: "{{ etcd_hosts[idx] }}"
---
apiVersion: "kubeadm.k8s.io/v1beta4"
kind: ClusterConfiguration
etcd:
    local:
        serverCertSANs:
        - "{{ etcd_hosts[idx] }}"
        peerCertSANs:
        - "{{ etcd_hosts[idx] }}"
        extraArgs:
        - name: initial-cluster
          value: {{ ''}} {%- for host in groups['etcd_nodes'] -%}
                   {{ hostvars[host].inventory_hostname }}=https://{{ hostvars[host].ansible_host }}:2380{% if not loop.last %},{% endif %}
                 {%- endfor -%}
                 {{ '' }}
        - name: initial-cluster-state
          value: new
        - name: name
          value: "{{ etcd_names[idx] }}"
        - name: listen-peer-urls
          value: "https://{{ etcd_hosts[idx] }}:2380"
        - name: listen-client-urls
          value: "https://{{ etcd_hosts[idx] }}:2379"
        - name: advertise-client-urls
          value: "https://{{ etcd_hosts[idx] }}:2379"
        - name: initial-advertise-peer-urls
          value: "https://{{ etcd_hosts[idx] }}:2380"

